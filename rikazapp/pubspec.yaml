name: rikazapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # iOS-style icons
  cupertino_icons: ^1.0.8

  # --- START: Additional Packages ---
  http: ^1.1.0             # For HTTP requests (API calls)
  shared_preferences: ^2.2.2  # For local data persistence
  slide_to_act: ^2.0.2     # For slide-to-connect button
  supabase_flutter: ^2.10.3
  # --- END: Additional Packages ---

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
